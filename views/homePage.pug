doctype html
html(lang='en')
    head
        title Home Page
        script(type='text/javascript').
            const foo = true;
            let bar = function() {};
            if (foo) {
                bar(1 + 5)
            }
    body
        h1 Jade - node template engine
        #container.col
            p This is a sample page
